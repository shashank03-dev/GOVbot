[phases.setup]
nixPkgs = ["python310", "nodejs"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "playwright install-deps chromium"
]

[start]
cmd = "uvicorn gov_agent.main:app --host 0.0.0.0 --port $PORT"
