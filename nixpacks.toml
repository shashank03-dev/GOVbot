[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "playwright install-deps chromium" 
]

[start]
cmd = "playwright install chromium && uvicorn gov_agent.main:app --host 0.0.0.0 --port $PORT"